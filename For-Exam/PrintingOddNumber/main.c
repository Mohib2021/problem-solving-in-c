#include <stdio.h>
int main()
{
    // Printing odd number from 5 to 35
    for (int i = 5; i <= 35; i++)
    {
        if (i % 2 != 0)
        {
            printf("%d \n", i);
        }
        }

    return 0;
}
