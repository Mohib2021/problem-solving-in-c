#include <stdio.h>

int main()
{
    // circle radius to area calculation
    int radius = 2;
    float PI = 3.142857;
    float area = PI * radius * radius;
    printf("The area of circle is %.2f", area);

    return 0;
}
